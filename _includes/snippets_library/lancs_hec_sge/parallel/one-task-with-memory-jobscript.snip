```
{{ site.remote.bash_shebang }}
{{ site.sched.comment }} {{ site.sched.flag.name }} serial-pi
{{ site.sched.comment }} {{ site.sched.flag.queue }} {{ site.sched.queue.testing }}
{{ site.sched.comment }} -l h_vmem=4G

# Configure the shell environment so that it matches the
# functionality of the login node
source /etc/profile

# Load the computing environment we need
module load anaconda3

# Execute the task
python pi.py 100000000
```
{: .language-bash}
