* `-pe <ncpus>` &mdash; How many CPUs does your job need? If you only need
  one CPU you can leave this out.

* `-l h_vmem=X` &mdash; How much memory per process does your job need? Where `X` is a number followed by M, G, or T to specify Mega, Giga or Terabytes; for example `-l h_vmem=1.5G`.
